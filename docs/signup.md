flowchart TD
    A["User visits /signup"] --> B["Signup.tsx: Already signed in?"]
    B -->|Yes| C["Redirect to /"]
    B -->|No| D["Render SignupCard (the form)"]
    D --> E["User fills: username, email, password, confirm"]
    E --> F["Clicks 'Create Account'"]
    F --> G["handleSubmit runs"]
    G --> H{"Passwords match?"}
    H -->|No| I["Show error: Passwords do not match"]
    H -->|Yes| J["signUp.create sends data to Clerk"]
    J -->|Clerk error| K["Show Clerk error message"]
    J -->|Success| L["prepareEmailAddressVerification"]
    L --> M["pendingVerification = true"]
    M --> N["UI switches to verification code form"]
    N --> O["User enters 6-digit code from email"]
    O --> P["handleVerify runs"]
    P --> Q["attemptEmailAddressVerification"]
    Q -->|Wrong code| R["Show error"]
    Q -->|Success| S["setActive creates session"]
    S --> T["navigate to / (Dashboard)"]
